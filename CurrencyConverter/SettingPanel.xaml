<UserControl x:Class="WoxPlugins.CurrencyConverter.SettingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Loaded="UserControl_Loaded"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="Gray" Margin="10" BorderThickness="1">
        <Grid Margin="10" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="APIKeyLabel" Grid.Column="0" Grid.Row="0" Text="API Key:"></TextBlock>
            <TextBox x:Name="APIKeyField" Grid.Column="1" Grid.Row="0"></TextBox>
            <TextBlock x:Name="InfoLabel" Grid.ColumnSpan="2" Grid.Row="1" Foreground="Gray" Margin="0 10 0 0"><Hyperlink Click="Hyperlink_Click">在此</Hyperlink>申请免费 API Key</TextBlock>
        </Grid>
    </Border>
</UserControl>
